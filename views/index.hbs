<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <link rel="stylesheet" href="/style.css">
    <title>🏫 Columbus Market Zone 🛒</title>
</head>

<body>
    <nav>
        <h4>Home Page</h4>
        <ul>
            <li><a href="/">Home</a></li>
            {{#if user }}
            <li><a href="/profile">Profile</a></li>
            <li><a href="/product">Products</a></li>
            <li><a href="/cart">Cart</a></li>
            <li><a href="/wishlist">Wishlist</a></li>
            <li><a href="/auth/logout">Logout</a></li>
            {{else}}
            <li><a href="/login">Login</a></li>
            <li><a href="/register">Register</a></li>
            {{/if}}
        </ul>
    </nav>
<div class="wrapper">
<!-- Attempting to add a search function. Adding in a specific way that it can work with actual data provided via database and routes -->
<input type="text" id="search" placeholder="Search">
<ul id="searchProducts">
    {{#each products}}
    <li>
        <a href="/product/{{this.id}}">
            <img src="{{this.image}}" alt="{{this.name}}">
            <h3>{{this.name}}</h3>
            <p>{{this.description}}</p>
            <p>{{this.price}}</p>
        </a>
    </li>
    {{/each}}
</ul>
</div>
    <div class="recentProducts">
    <!-- Need to add list of items on homepage  
        <h3>Recent Products</h3>
        <ul>
            {{#each products}}
            <li>
                <a href="/product/{{this.id}}">
                    <img src="{{this.image}}" alt="{{this.name}}">
                    <h3>{{this.name}}</h3>
                    <p>{{this.description}}</p>
                    <p>{{this.price}}</p>
                </a>
            </li>
            {{/each}}
        </ul>
        -->
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"
                 integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p"
                 crossorigin="anonymous">
    </script>
</body>

</html>